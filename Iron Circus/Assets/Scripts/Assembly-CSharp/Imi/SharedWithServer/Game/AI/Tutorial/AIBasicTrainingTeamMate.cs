using Imi.SharedWithServer.Game.AI;

namespace Imi.SharedWithServer.Game.AI.Tutorial
{
	public class AIBasicTrainingTeamMate : AIStateMachine
	{
		public AIBasicTrainingTeamMate(GameEntity playerEntity, AIDifficulty difficulty, AIRole role, AICache cache) : base(default(GameEntity), default(AIDifficulty), default(AIRole), default(AICache))
		{
		}

	}
}
