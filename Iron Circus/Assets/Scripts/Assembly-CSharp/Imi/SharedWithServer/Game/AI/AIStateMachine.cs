namespace Imi.SharedWithServer.Game.AI
{
	public class AIStateMachine : BaseAI
	{
		public AIStateMachine(GameEntity playerEntity, AIDifficulty difficulty, AIRole role, AICache cache) : base(default(GameEntity), default(AIDifficulty), default(AIRole), default(AICache))
		{
		}

	}
}
