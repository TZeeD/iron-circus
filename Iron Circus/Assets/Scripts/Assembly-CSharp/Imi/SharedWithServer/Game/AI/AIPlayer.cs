namespace Imi.SharedWithServer.Game.AI
{
	public class AIPlayer : AIStateMachine
	{
		public AIPlayer(GameEntity playerEntity, AIDifficulty difficulty, AIRole role, AICache cache) : base(default(GameEntity), default(AIDifficulty), default(AIRole), default(AICache))
		{
		}

	}
}
