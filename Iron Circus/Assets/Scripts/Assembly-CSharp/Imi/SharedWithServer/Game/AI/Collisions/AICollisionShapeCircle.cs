using Jitter.LinearMath;

namespace Imi.SharedWithServer.Game.AI.Collisions
{
	public class AICollisionShapeCircle : AICollisionShape
	{
		public AICollisionShapeCircle(JVector position, float radius, bool isTrigger, GameEntity entity) : base(default(bool), default(GameEntity))
		{
		}

	}
}
