using Imi.SharedWithServer.Game.AI;

namespace Imi.SharedWithServer.Game.AI.States
{
	public class AIStateDefault : AIStateBase
	{
		public AIStateDefault(AIStateMachine owner, AICache cache) : base(default(AIStateMachine), default(AICache))
		{
		}

	}
}
