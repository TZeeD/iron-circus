using Imi.SharedWithServer.Game.AI;

namespace SteelCircus.AI.Testing
{
	public class LocalTestAI : AIPlayer
	{
		public LocalTestAI(GameEntity playerEntity, AIDifficulty difficulty, AIRole role, AICache cache) : base(default(GameEntity), default(AIDifficulty), default(AIRole), default(AICache))
		{
		}

	}
}
