using System;

namespace unittests.server.SharedWithServer.Networking.Netcode.Core
{
	public struct NetcodeServerConfig
	{
		public NetcodeServerConfig(int maxPeers, ulong protocolId, byte[] privateKey) : this()
		{
		}

	}
}
